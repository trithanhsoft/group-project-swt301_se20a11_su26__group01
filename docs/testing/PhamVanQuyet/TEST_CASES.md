# Test Cases Detail Table - Phạm Văn Quyết (DE190425)

| Test Case ID | Test Level | Technique | Preconditions | Test Data | Steps | Expected Result | Actual Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_REG_001** | Component | Equivalence Partitioning | Render Register.jsx | fullName: 'Phạm Văn Quyết', email: 'de190425@fpt.edu.vn', password: 'abcdef', confirm: 'abcdeg' | 1. Điền thông tin vào form.<br>2. Click nút "Đăng ký". | Hiển thị lỗi "Mật khẩu xác nhận không khớp". Không gọi API và navigate. | Hiển thị lỗi "Mật khẩu xác nhận không khớp". Không gọi API và navigate. | PASSED |
| **TC_REG_002** | Component | Boundary Value Analysis | Render Register.jsx | password: 'abcde', confirm: 'abcde' | 1. Điền mật khẩu 5 ký tự.<br>2. Click nút "Đăng ký". | Hiển thị lỗi "Mật khẩu phải ít nhất 6 ký tự". Không gọi API và navigate. | Hiển thị lỗi "Mật khẩu phải ít nhất 6 ký tự". Không gọi API và navigate. | PASSED |
| **TC_REG_003** | Component | Boundary Value Analysis | Render Register.jsx | password: 'abcdef', confirm: 'abcdef', API resolved success | 1. Điền mật khẩu đúng biên 6 ký tự.<br>2. Click nút "Đăng ký". | Gọi API và navigate sang trang `/login`. | Gọi API và navigate sang trang `/login`. | PASSED |
| **TC_REG_004** | Component | Boundary Value Analysis | Render Register.jsx | password: 'abcdefg', confirm: 'abcdefg', API resolved success | 1. Điền mật khẩu trên biên 7 ký tự.<br>2. Click nút "Đăng ký". | Gọi API và navigate sang trang `/login`. | Gọi API và navigate sang trang `/login`. | PASSED |
| **TC_REG_005** | Component | Data Flow Testing | Render Register.jsx | fullName: 'Phạm Văn Quyết', email: 'de190425@fpt.edu.vn', password: 'abcdef', confirm: 'abcdef' | 1. Điền form đầy đủ.<br>2. Click nút "Đăng ký". | Gửi chính xác đối tượng `{ username: 'Phạm Văn Quyết', email: 'de190425@fpt.edu.vn', password: 'abcdef' }` sang AuthContext. | Gửi chính xác đối tượng đã mô tả lên AuthContext. | PASSED |
| **TC_REG_006** | Component | Equivalence Partitioning | Render Register.jsx | API resolved: `{ success: false, message: 'Email đã tồn tại' }` | 1. Điền form hợp lệ.<br>2. Click nút "Đăng ký". | Hiển thị lỗi "Email đã tồn tại". Không navigate. | Hiển thị lỗi "Email đã tồn tại". Không navigate. | PASSED |
| **TC_REG_007** | Component | Equivalence Partitioning | Render Register.jsx | API resolved: `{ success: false }` (không message) | 1. Điền form hợp lệ.<br>2. Click nút "Đăng ký". | Hiển thị lỗi mặc định "Đăng ký thất bại". Không navigate. | Hiển thị lỗi mặc định "Đăng ký thất bại". Không navigate. | PASSED |
| **TC_REG_008** | Component | State Transition Testing | Render Register.jsx | API resolved: promise trì hoãn | 1. Điền form hợp lệ.<br>2. Click nút "Đăng ký".<br>3. Kiểm tra nút submit khi API đang chờ.<br>4. Chờ API hoàn thành. | Nút submit bị disabled và hiển thị spinner trong lúc loading. Sau khi API trả về, nút enable trở lại. | Nút submit disabled kèm spinner khi đang xử lý, tự động enable khi kết thúc. | PASSED |
| **TC_REG_009** | Component | Decision Coverage | Render Register.jsx | password: 'abc', confirm: 'abcd' (mismatch + short) | 1. Điền form sai cả 2 quy tắc.<br>2. Click nút "Đăng ký". | Hiển thị lỗi không khớp trước ("Mật khẩu xác nhận không khớp") theo đúng thứ tự logic mã nguồn. | Hiển thị lỗi không khớp mật khẩu trước. | PASSED |
| **TC_REG_010** | Component | Statement Coverage | Render Register.jsx | Click button toggle password | 1. Nhập mật khẩu (ẩn).<br>2. Click button toggle 👁️.<br>3. Click lần nữa. | Type của input đổi từ "password" -> "text" -> "password". | Type của input thay đổi đúng trạng thái. | PASSED |
| **TC_REG_011** | Component | Statement Coverage | Render Register.jsx | Click button toggle confirm password | 1. Nhập xác nhận (ẩn).<br>2. Click button toggle 👁️.<br>3. Click lần nữa. | Type của input đổi từ "password" -> "text" -> "password". | Type của input thay đổi đúng trạng thái. | PASSED |
| **TC_REG_012** | Component | Time-based Testing | Render Register.jsx | Click Google/Facebook | 1. Click nút đăng ký Google.<br>2. Kiểm tra thông báo hiển thị.<br>3. Giả lập thời gian trôi qua 2500ms. | Hiển thị "Tính năng đang phát triển", sau 2500ms thông báo tự động biến mất. | Hiển thị thông báo và tự động ẩn đi đúng thời gian. | PASSED |
| **TC_AUTH_REG_001** | Unit | Path Testing | Render Hook AuthProvider | API.post resolved: user data | Gọi hàm `register` với thông tin hợp lệ. | API gửi POST request tới `/auth/register`. Trả về đối tượng thành công. | API nhận cuộc gọi chính xác và trả về thông tin thành công. | PASSED |
| **TC_AUTH_REG_002** | Unit | Decision Coverage | Render Hook AuthProvider | API.post rejected: string data ('Email đã tồn tại') | Gọi hàm `register`. | Trả về `success: false` kèm `message: 'Email đã tồn tại'`. | Trả về success false kèm lỗi chuỗi. | PASSED |
| **TC_AUTH_REG_003** | Unit | Decision Coverage | Render Hook AuthProvider | API.post rejected: object có message | Gọi hàm `register`. | Trả về `success: false` kèm `message` lấy từ đối tượng lỗi API. | Trả về success false kèm message lỗi. | PASSED |
| **TC_AUTH_REG_004** | Unit | Decision Coverage | Render Hook AuthProvider | API.post rejected: object có detail | Gọi hàm `register`. | Trả về `success: false` kèm `message` lấy từ detail của lỗi API. | Trả về success false kèm detail lỗi. | PASSED |
| **TC_AUTH_REG_005** | Unit | Path Testing | Render Hook AuthProvider | API.post rejected: Network Error | Gọi hàm `register`. | Trả về `success: false` kèm message mặc định "Đăng ký thất bại". | Trả về success false kèm lỗi mặc định. | PASSED |
